// Configuration for compilation ts on client side
const config = {
    // here the options of mysql connection need to be added, and the basic connection
    mySql: {
        options: {},
        url: {

        }
    },
    server: {
        host: "localhost",
        port: 3000

    }
}